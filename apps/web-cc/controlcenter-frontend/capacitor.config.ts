import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'controlcenter-frontend',
  webDir: 'dist/controlcenter-frontend/browser'
};

export default config;
